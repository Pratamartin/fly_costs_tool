"use strict";

var _zodToOpenapi = require("@asteasolutions/zod-to-openapi");
var _zodOpenapi = require("@hono/zod-openapi");
var _vitest = require("vitest");
var _createErrorSchema = _interopRequireDefault(require("./create-error-schema.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
(0, _vitest.describe)("create-error-schema", () => {
  (0, _vitest.it)("creates error schema for string schema", () => {
    const stringSchema = _zodOpenapi.z.string();
    const errorSchema = (0, _createErrorSchema.default)(stringSchema);
    const result = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: ["fieldName"],
          message: "Expected string, received undefined"
        }]
      }
    });
    (0, _vitest.expect)(result.success).toBe(true);
    (0, _vitest.expect)(result.data?.success).toBe(false);
    (0, _vitest.expect)(result.data?.error.name).toBe("ZodError");
    (0, _vitest.expect)(result.data?.error.issues).toHaveLength(1);
    (0, _vitest.expect)(result.data?.error.issues[0].code).toBe("invalid_type");
  });
  (0, _vitest.it)("creates error schema for object schema", () => {
    const objectSchema = _zodOpenapi.z.object({
      name: _zodOpenapi.z.string(),
      age: _zodOpenapi.z.number()
    });
    const errorSchema = (0, _createErrorSchema.default)(objectSchema);
    const result = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: ["name"],
          message: "Expected string, received undefined"
        }, {
          code: "invalid_type",
          path: ["age"],
          message: "Expected number, received undefined"
        }]
      }
    });
    (0, _vitest.expect)(result.success).toBe(true);
    (0, _vitest.expect)(result.data?.success).toBe(false);
    (0, _vitest.expect)(result.data?.error.issues).toHaveLength(2);
  });
  (0, _vitest.it)("creates error schema for array schema", () => {
    const arraySchema = _zodOpenapi.z.array(_zodOpenapi.z.string());
    const errorSchema = (0, _createErrorSchema.default)(arraySchema);
    const result = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: [0],
          message: "Expected string, received number"
        }]
      }
    });
    (0, _vitest.expect)(result.success).toBe(true);
    (0, _vitest.expect)(result.data?.success).toBe(false);
    (0, _vitest.expect)(result.data?.error.issues[0].path).toEqual([0]);
  });
  (0, _vitest.it)("validates error schema structure", () => {
    const schema = _zodOpenapi.z.string();
    const errorSchema = (0, _createErrorSchema.default)(schema);
    const validResult1 = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: ["field"],
          message: "Expected string, received undefined"
        }]
      }
    });
    (0, _vitest.expect)(validResult1.success).toBe(true);
    const validResult2 = errorSchema.safeParse({
      success: true,
      error: {
        name: "ZodError",
        issues: []
      }
    });
    (0, _vitest.expect)(validResult2.success).toBe(true);
    const invalidResult1 = errorSchema.safeParse({
      error: {
        name: "ZodError",
        issues: []
      }
    });
    (0, _vitest.expect)(invalidResult1.success).toBe(false);
    const invalidResult2 = errorSchema.safeParse({
      success: false
    });
    (0, _vitest.expect)(invalidResult2.success).toBe(false);
    const invalidResult3 = errorSchema.safeParse({
      success: "not-a-boolean",
      error: {
        name: "ZodError",
        issues: []
      }
    });
    (0, _vitest.expect)(invalidResult3.success).toBe(false);
  });
  (0, _vitest.it)("handles issues with optional message field", () => {
    const schema = _zodOpenapi.z.string();
    const errorSchema = (0, _createErrorSchema.default)(schema);
    const result = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: ["field"]
        }]
      }
    });
    (0, _vitest.expect)(result.success).toBe(true);
    (0, _vitest.expect)(result.data?.error.issues[0].message).toBeUndefined();
  });
  (0, _vitest.it)("handles mixed path types (string and number)", () => {
    const schema = _zodOpenapi.z.object({
      items: _zodOpenapi.z.array(_zodOpenapi.z.string())
    });
    const errorSchema = (0, _createErrorSchema.default)(schema);
    const result = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: [{
          code: "invalid_type",
          path: ["items", 0],
          message: "Expected string, received number"
        }]
      }
    });
    (0, _vitest.expect)(result.success).toBe(true);
    (0, _vitest.expect)(result.data?.error.issues[0].path).toEqual(["items", 0]);
  });
  (0, _vitest.it)("validates that success field accepts both true and false", () => {
    const schema = _zodOpenapi.z.string();
    const errorSchema = (0, _createErrorSchema.default)(schema);
    const result1 = errorSchema.safeParse({
      success: true,
      error: {
        name: "ZodError",
        issues: []
      }
    });
    (0, _vitest.expect)(result1.success).toBe(true);
    const result2 = errorSchema.safeParse({
      success: false,
      error: {
        name: "ZodError",
        issues: []
      }
    });
    (0, _vitest.expect)(result2.success).toBe(true);
  });
  (0, _vitest.it)("ensures example shape has no extra properties", () => {
    _zodToOpenapi.zodToOpenAPIRegistry.clear();
    const schema = _zodOpenapi.z.object({
      name: _zodOpenapi.z.string(),
      age: _zodOpenapi.z.number()
    });
    (0, _createErrorSchema.default)(schema);
    const metadata = [..._zodToOpenapi.zodToOpenAPIRegistry._map.values()][1];
    (0, _vitest.expect)(metadata).toBeDefined();
    (0, _vitest.expect)(metadata.example).toBeDefined();
    const {
      example
    } = metadata;
    (0, _vitest.expect)(example).toHaveProperty("name");
    (0, _vitest.expect)(example).toHaveProperty("issues");
    (0, _vitest.expect)(Object.keys(example)).toHaveLength(2);
    (0, _vitest.expect)(Array.isArray(example.issues)).toBe(true);
    example.issues.forEach(issue => {
      (0, _vitest.expect)(issue).toHaveProperty("code");
      (0, _vitest.expect)(issue).toHaveProperty("path");
      const expectedKeys = ["code", "path"];
      if (issue.message !== void 0) {
        expectedKeys.push("message");
      }
      (0, _vitest.expect)(Object.keys(issue)).toEqual(expectedKeys);
    });
  });
  (0, _vitest.it)("ensures example shape is array for array schemas", () => {
    _zodToOpenapi.zodToOpenAPIRegistry.clear();
    const schema = _zodOpenapi.z.array(_zodOpenapi.z.string());
    (0, _createErrorSchema.default)(schema);
    const metadata = [..._zodToOpenapi.zodToOpenAPIRegistry._map.values()][1];
    (0, _vitest.expect)(metadata).toBeDefined();
    (0, _vitest.expect)(metadata.example).toBeDefined();
    const {
      example
    } = metadata;
    (0, _vitest.expect)(example).toHaveProperty("name");
    (0, _vitest.expect)(example).toHaveProperty("issues");
    (0, _vitest.expect)(Object.keys(example)).toHaveLength(2);
    (0, _vitest.expect)(Array.isArray(example.issues)).toBe(true);
    example.issues.forEach(issue => {
      (0, _vitest.expect)(issue).toHaveProperty("code");
      (0, _vitest.expect)(issue).toHaveProperty("path");
      const expectedKeys = ["code", "path"];
      if (issue.message !== void 0) {
        expectedKeys.push("message");
        (0, _vitest.expect)(issue.message).toMatch(/^Invalid input: expected (.*), received (.*)$/);
      }
      (0, _vitest.expect)(Object.keys(issue)).toEqual(expectedKeys);
    });
  });
});
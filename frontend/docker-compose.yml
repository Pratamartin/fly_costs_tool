services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fly-costs-frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
